import { HoverableDirective } from './hoverable.directive';

describe('HoverableDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverableDirective();
    expect(directive).toBeTruthy();
  });
});
