import { TwowayexampleDirective } from './twowayexample.directive';

describe('TwowayexampleDirective', () => {
  it('should create an instance', () => {
    const directive = new TwowayexampleDirective();
    expect(directive).toBeTruthy();
  });
});
